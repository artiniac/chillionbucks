import * as T from './vendor/three.module.js';
import {mesh,orb,block,makePerson} from './chilltopia-models.js?v=20260907-release3';
const water=new T.MeshPhysicalMaterial({color:'#64cbdc',transparent:true,opacity:.48,roughness:.12,metalness:.02,side:T.DoubleSide,depthWrite:false});
water.userData.shared=true;
function pipe(g,a,b,r=.06,color='#adc4c0'){const start=new T.Vector3(...a),end=new T.Vector3(...b),d=end.clone().sub(start);const m=mesh(g,new T.CylinderGeometry(r,r,d.length(),10),color);m.position.copy(start.addScaledVector(d,.5));m.quaternion.setFromUnitVectors(new T.Vector3(0,1,0),d.normalize());return m;}
export function palm(){const g=new T.Group();const trunk=new T.CatmullRomCurve3([new T.Vector3(0,0,0),new T.Vector3(.15,2,0),new T.Vector3(.45,4.2,.1)]);mesh(g,new T.TubeGeometry(trunk,12,.15,9,false),'#92785a');for(let i=0;i<12;i++){const a=i*Math.PI*2/12,pts=[],indices=[];for(let j=0;j<=12;j++){const q=j/12,r=q*2.5,w=Math.sin(q*Math.PI)*.28;const x=.45+Math.cos(a)*r,y=4.2+Math.sin(q*Math.PI)*.6-q*q*.9,z=.1+Math.sin(a)*r;pts.push(x-Math.sin(a)*w,y,z+Math.cos(a)*w,x+Math.sin(a)*w,y,z-Math.cos(a)*w);if(j<12){const k=j*2;indices.push(k,k+1,k+2,k+1,k+3,k+2);}}const geo=new T.BufferGeometry();geo.setAttribute('position',new T.Float32BufferAttribute(pts,3));geo.setIndex(indices);geo.computeVertexNormals();mesh(g,geo,new T.MeshStandardMaterial({color:i%2?'#39714d':'#63864a',side:T.DoubleSide,roughness:.8}));}for(let i=0;i<3;i++)orb(g,'#745439',.4+Math.cos(i*2)*.2,4,Math.sin(i*2)*.2,.16);return g;}
export const ATTRACTIONS={bucket:'Tipping-bucket tower',mushroom:'Rain mushroom',jets:'Splash arch',pool:'Lagoon pool',icecream:'Ice cream stand',burger:'Burger grill',umbrella:'Shady picnic spot',palm:'Royal palm'};
export function attraction(type){if(type==='palm')return palm();const g=new T.Group();
 if(type==='bucket'){
  block(g,'#c9c3aa',0,.1,0,5,.2,4.5);for(const x of [-1.45,1.45])for(const z of [-1,1])pipe(g,[x,.1,z],[x,5.5,z],.12,'#497c79');block(g,'#bc9370',0,3.2,0,3.4,.2,2.6);
  for(const z of [-1.25,1.25]){pipe(g,[-1.7,4.1,z],[1.7,4.1,z],.045);for(let i=0;i<9;i++)pipe(g,[-1.6+i*.4,3.3,z],[-1.6+i*.4,4.1,z],.025);}
  for(let i=0;i<11;i++)block(g,'#c8b18a',-2.1,.15+i*.29,1.8-i*.25,1,.12,.35);pipe(g,[-2.6,.8,1.8],[-2.6,3.8,-.7],.04);
  const pivot=new T.Group();pivot.position.set(0,5.6,0);g.add(pivot);mesh(pivot,new T.CylinderGeometry(1.1,.7,1.5,28,1,true),new T.MeshStandardMaterial({color:'#d8a638',metalness:.25,roughness:.3,side:T.DoubleSide}),0,.1,0);mesh(pivot,new T.CylinderGeometry(.7,.7,.08,28),'#b18433',0,-.65,0);const fill=mesh(pivot,new T.CylinderGeometry(.95,.65,1.1,28),water,0,-.1,0);pipe(g,[-1.5,5.65,0],[1.5,5.65,0],.07,'#59726f');
  pipe(g,[1.9,0,-1.3],[1.9,7,-1.3],.09);pipe(g,[1.9,7,-1.3],[0,7,-1.3],.09);const feed=pipe(g,[0,6.9,-1.3],[0,5.9,-.4],.04,water);
  const pour=mesh(g,new T.CylinderGeometry(.5,1.7,4.4,20,1,true),water,1,2.6,0);pour.rotation.z=.2;const splash=mesh(g,new T.TorusGeometry(1,.08,6,32),water,1,.22,0);splash.rotation.x=Math.PI/2;
  g.userData.anim={type,pivot,fill,pour,splash,feed};
 }else if(type==='mushroom'){
  mesh(g,new T.CylinderGeometry(2.6,2.7,.18,40),'#d3cbb2',0,.09,0);pipe(g,[0,.1,0],[0,2.4,0],.19,'#73a19b');mesh(g,new T.ConeGeometry(2.2,.7,40),'#bd6651',0,2.6,0);const rain=new T.Group();for(let i=0;i<28;i++){const a=i*6.283/28;pipe(rain,[Math.cos(a)*2.1,2.35,Math.sin(a)*2.1],[Math.cos(a)*2.3,.18,Math.sin(a)*2.3],.018,water);}g.add(rain);g.userData.anim={type,rain};
 }else if(type==='jets'){
  block(g,'#cec8b2',0,.05,0,5,.1,2);const streams=[];for(let k=0;k<3;k++){const z=(k-1)*.55;for(const sign of [-1,1])pipe(g,[sign*2,.05,z],[sign*2,.45,z],.08,'#4f8984');const curve=new T.CatmullRomCurve3([new T.Vector3(-2,.4,z),new T.Vector3(0,2.6,z),new T.Vector3(2,.4,z)]);streams.push(mesh(g,new T.TubeGeometry(curve,28,.035,6,false),water));}g.userData.anim={type,streams};
 }else if(type==='pool'){
  mesh(g,new T.CylinderGeometry(4.1,4.2,.35,48),'#d8d1bb',0,.1,0,1,1,.7);mesh(g,new T.CylinderGeometry(3.85,3.85,.08,48),new T.MeshPhysicalMaterial({color:'#278eaa',roughness:.15,metalness:.1,clearcoat:1}),0,.3,0,1,1,.7);const swimmers=[];for(let i=0;i<3;i++){const r=new T.Group();mesh(r,new T.TorusGeometry(.3,.1,8,18),['#dfb354','#ca7869','#7ab5a0'][i],0,.05,0).rotation.x=Math.PI/2;const person=makePerson();person.scale.setScalar(.4);r.add(person);g.add(r);swimmers.push(r);}g.userData.anim={type,swimmers};
 }else if(type==='icecream'||type==='burger'){
  block(g,'#ad8970',0,1.1,0,3.4,2.2,2.2);block(g,'#3d5c58',0,1.25,1.115,2.7,1.15,.04);block(g,'#ece0c5',0,.82,1.3,3.5,.13,.65);for(const x of [-1.7,1.7])pipe(g,[x,.1,1.5],[x,2.7,1.5],.05,'#7b8070');for(let i=0;i<8;i++)block(g,i%2?'#eee4c9':type==='icecream'?'#71b1aa':'#bb7158',-1.575+i*.45,2.5,.3,.45,.17,3.1);
  block(g,'#244c42',0,2.85,1.65,2.6,.48,.12);for(let i=0;i<3;i++)orb(g,'#f4d394',-.5+i*.5,2.85,1.74,.05);
  if(type==='icecream'){mesh(g,new T.ConeGeometry(.43,1.1,16),'#d2ad6e',0,3.5,.2).rotation.z=Math.PI;orb(g,'#ecd1bd',0,4.05,.2,.55);orb(g,'#de99a6',.15,4.35,.2,.35);}else{orb(g,'#c79b54',0,3.5,.2,.75,.3,.6);block(g,'#74934e',0,3.4,.2,1.45,.09,1.1);block(g,'#654b32',0,3.3,.2,1.3,.15,1);orb(g,'#c9974c',0,3.18,.2,.74,.15,.6);}
  const server=makePerson();server.position.set(0,.05,.6);server.scale.setScalar(.85);g.add(server);
 }else if(type==='umbrella'){
  pipe(g,[0,0,0],[0,2.7,0],.055,'#b19a77');mesh(g,new T.ConeGeometry(2,.65,12),'#ece0c3',0,2.75,0);block(g,'#ae9270',0,.78,0,2.1,.12,1.1);for(const z of [-.95,.95]){block(g,'#ad916d',0,.43,z,2.2,.1,.35);for(const x of [-.75,.75])block(g,'#61766b',x,.2,z,.1,.4,.3);}
 }
 return g;
}
export function animateAttraction(g,time){const a=g.userData.anim;if(!a)return;const cycle=((time+(g.userData.offset||0))%18)/18;if(a.type==='bucket'){const tipping=cycle>.72&&cycle<.93;a.pivot.rotation.z=tipping?Math.sin((cycle-.72)/.21*Math.PI)*1.7:0;a.fill.scale.y=Math.max(.03,cycle<.72?cycle/.72:1-(cycle-.72)/.21);a.fill.position.y=-.58+a.fill.scale.y*.55;a.pour.visible=tipping&&a.pivot.rotation.z>.7;a.splash.visible=a.pour.visible;a.splash.scale.setScalar(1+Math.sin(time*9)*.15);a.feed.visible=!tipping;}else if(a.type==='mushroom')a.rain.scale.y=.96+Math.sin(time*7)*.025;else if(a.type==='jets')a.streams.forEach((m,i)=>m.scale.y=.8+Math.sin(time*2+i)*.18);else if(a.type==='pool')a.swimmers.forEach((r,i)=>{const angle=time*.1+i*2.1;r.position.set(Math.cos(angle)*2.5,.4,Math.sin(angle)*1.5);r.rotation.y=-angle;});}
export function surfaceTexture(type){const canvas=document.createElement('canvas');canvas.width=canvas.height=256;const c=canvas.getContext('2d');c.fillStyle=type==='grass'?'#708953':'#c9c2ab';c.fillRect(0,0,256,256);if(type==='grass'){for(let i=0;i<28000;i++){const x=Math.random()*256,y=Math.random()*256,v=40+Math.random()*70;c.strokeStyle=`rgba(${v*.8},${v+25},${v*.55},.3)`;c.beginPath();c.moveTo(x,y);c.lineTo(x+Math.random()*3,y-Math.random()*5);c.stroke();}}else{for(let y=0;y<8;y++)for(let x=0;x<8;x++){const v=177+Math.random()*35;c.fillStyle=`rgb(${v+8},${v+4},${v-10})`;c.fillRect(x*32+1,y*32+1,30,30);}for(let i=0;i<8000;i++){c.fillStyle=Math.random()>.5?'#ffffff10':'#0000000d';c.fillRect(Math.random()*256,Math.random()*256,1,1);}}const texture=new T.CanvasTexture(canvas);texture.wrapS=texture.wrapT=T.RepeatWrapping;texture.colorSpace=T.SRGBColorSpace;texture.repeat.set(type==='grass'?14:6,type==='grass'?14:6);texture.anisotropy=4;return texture;}
